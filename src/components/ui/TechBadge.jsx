export default function TechBadge({ children }) {
  return (
    <span className="inline-block bg-sage/10 border border-sage/15 rounded-full px-3 py-1 text-xs font-medium text-sage-dark">
      {children}
    </span>
  )
}
