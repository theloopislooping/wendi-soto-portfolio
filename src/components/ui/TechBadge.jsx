export default function TechBadge({ children }) {
  return (
    <span className="inline-block bg-white/4 border border-white/8 rounded-full px-3 py-1 text-xs font-medium text-sage-dark">
      {children}
    </span>
  )
}
